# Welcome to the Meowchallenge

Steps to run the app (In different terminals):
1. "Npm install && Expo start" in the "meow-front" directory
2. "Npm install && npm start" in the "meow-back" directory

